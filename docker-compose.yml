services:
  hello_sdl:
    build: .

    image: "hello_sdl:latest"
    volumes:
      - ./HELLO_SDL:/HELLO_SDL

    environment:
      - DISPLAY=${DISPLAY}
      - WAYLAND_DISPLAY=${WAYLAND_DISPLAY}
      - SDL_VIDEODRIVER=wayland

    stdin_open: true
    tty: true
